import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    cardsContainer: {
        marginTop: 16,
    },
    removedContainer: {
        alignItems: "center",
        marginBottom: 20,
    },
    cardImage: {
        width: 50,
        height: 50,
        marginRight: 10,
    },
    removedText: {
        fontSize: 16,
        color: "red",
        marginTop: 8,
    },
})