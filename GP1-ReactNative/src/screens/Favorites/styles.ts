import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    cardsContainer: {
        marginTop: 20,
        width: "100%",
        padding: 10,
    },
    removedContainer: {
        alignItems: "center",
        marginBottom: 20,
    },
})