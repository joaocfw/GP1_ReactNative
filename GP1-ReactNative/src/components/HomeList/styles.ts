import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    movieListContainer: {
        flexDirection: 'column',
    },
    flatListContent: {
        paddingLeft: 12,
        paddingTop: 12,
    },
})