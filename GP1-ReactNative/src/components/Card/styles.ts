import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	CardContainer: {
        width: 130,
        height: 200,
        borderRadius: 8,
        marginRight: 15,
        overflow:'hidden',
    },
    CardImage: {
        width: '100%',
        height: '100%',
    },
})