import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	CardContainer: {
        width: 123,
        height: 200,
        borderRadius: 8,
        overflow:'hidden',
    },
    CardImage: {
        width: '100%',
        height: '100%',
    },
})