import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	movieCardContainer: {
        width: 150,
        height: 200,
        borderRadius: 8,
        marginRight: 15,
        overflow:'hidden',
    },
    movieCardImage: {
        width: '100%',
        height: '100%',
    },
})